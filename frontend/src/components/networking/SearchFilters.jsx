// SearchFilters is now built directly into the Networking page.
// This file is kept for backward compatibility if imported elsewhere.
import React from "react";

export default function SearchFilters({ onFilterChange }) {
  return null; // Logic moved into Networking.jsx
}